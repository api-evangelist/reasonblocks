generated: '2026-07-21'
method: searched
source: https://docs.reasonblocks.com/api-reference/rest-api/versioning
versioning:
  scheme: uri-path
  current: v1
  docs: https://docs.reasonblocks.com/api-reference/rest-api/versioning
  guarantees:
  - Request schemas only grow in backwards-compatible ways (new optional fields ok, new required fields are breaking).
  - Response schemas only grow additively; removing/renaming a field is breaking.
  - HTTP status-code semantics are stable per route.
  - Path parameter names are stable.
  legacy_aliases: >-
    Unversioned paths (e.g. POST /traces/retrieve) remain mounted for already-deployed
    SDK clients but are hidden from the OpenAPI schema and may be removed after a
    deprecation window.
deprecation:
  policy_url: https://docs.reasonblocks.com/api-reference/rest-api/versioning
  policy: >-
    Breaking changes ship under a new /v2/ prefix mounted alongside /v1/, with a
    deprecation window announced before the old surface is sunset. Unversioned legacy
    aliases may be removed after a deprecation window.
  sunset_header: false
sla:
  url: null
  uptime_target: null
status_page: null
health_endpoint: https://rb-api.reasonblocks.com/health
deprecated_operations: []
